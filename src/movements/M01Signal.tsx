/**
 * 01 — Signal d'entrée (INTENSITÉ).
 * Le HERO : le cœur pixelisé qui bat (composé avec le Capitaine, 16/07/2026).
 * Voir src/hero/ — TempoHero et ses sous-composants.
 */

import { TempoHero } from "../hero/TempoHero";

export function M01Signal() {
  return <TempoHero />;
}
